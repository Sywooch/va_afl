before_script:
  - composer global require "fxp/composer-asset-plugin:^1.2.0"
  - composer install --prefer-dist --no-interaction
stages:
  - test
  - deploy
test:
  script:
    - echo "OK"
deploy:
  script:
    - rsync -rltzuvO ./ daitel@va-transaero.ru:/var/www/vag_afl
  only:
    - master